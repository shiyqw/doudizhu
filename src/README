autorun是生成对局的程序，运行以后会生成三个dat文件（请忽略打到屏幕上的输出，仅供调试用）。生成以后可以根据dat文件更新权值，更新后的权值替换原autorun的weight数组，再次运行即可。
ddz_greedy是可以游戏的贪心程序，可以直接上传botzone，目前简单测试下来没有bug，不过还需要进一步验证。
ddz是最新的萌卡，目测水平至少是Botzone前三，不过需要测试有没有bug。毕竟代码正确性没有那么肯定。
